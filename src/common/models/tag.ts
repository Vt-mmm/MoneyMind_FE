export interface TagInfo {
    id: string;            // Mã định danh duy nhất của tag
    name: string;          // Tên của tag
    description: string;   // Mô tả chi tiết về tag
    color: string;         // Mã màu đại diện cho tag (HEX)
  }
  